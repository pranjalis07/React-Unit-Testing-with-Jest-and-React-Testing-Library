/*
import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

//test or it can be used 
test('renders learn react link', () => { //call back function 
  render(<App/>); //render 
  const linkElement = screen.getByText(/learn react/i); //find the element //no interaction here 
  expect(linkElement).toBeInTheDocument(); //results are as expected or not 
});

//screen. as lot many functionalities getAll, get  and findAll, find, queryBy 
//difference ----> if more than 1 match findBy getBy returns error but others not 
//difference between findby and getby ---> getby dont support async await , findby support it 

//priority order -- refer diagram 
//Accessible to everyone  then semantic queries then test id 


*/